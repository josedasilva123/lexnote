import styled from "styled-components";

export const StyledDashboardForm = styled.aside`
    gap: 1rem;
    padding: 1.5rem 1.5rem 2rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    .content{
        align-items: flex-start;
        gap: 1rem;
    }
`